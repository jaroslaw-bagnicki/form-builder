import React from 'react';

// Navbar
export default () => (
  <nav className="nav-wrapper grey">
    <div className="container">
      <a href="/" className="brand-logo">Form Builder</a>
    </div>
  </nav>
);
